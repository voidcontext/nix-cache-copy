ignore_merge_commits = false
branch_whitelist = []
pre_bump_hooks = [
  "cargo set-version {{version}}"
]
post_bump_hooks = []
tag_prefix = "v"

[commit_types]

[changelog]
path = "CHANGELOG.md"
authors = []

[bump_profiles]
